# Homepage SEO & Content Update — 10 Changes

Most of this brief is already live on the homepage from earlier work. This plan states, per change, what exists and what actually needs editing, so nothing gets duplicated.

Two decisions applied throughout (from your answers):
- Free shipping threshold stays **₹999** (the brief's "Rs. 2,999" is not used).
- All "shop" links point to the existing collections grid (`/#collections`), since there is no standalone `/products` listing page — only `/products/:handle` per bag.

## Change 1 — H1 tag
Already present in the hero: the H1 contains "Because Devotion Never Ends." plus the line "Handcrafted Potli Bags Online — Shagun, Bridal & Wedding Gifting".

Edit: make the keyword line the visually dominant part of the H1 — increase its size and weight so it reads as the largest heading on the page, keeping the brand line above it as a smaller kicker inside the same H1.

## Change 2 — Hero paragraph + 2 CTAs
The paragraph is already there, matching your text (currently "Whether you're gifting…"; will be set to your exact wording "Whether gifting…").

Edits:
- Replace the two current hero CTAs with: **Shop All Potli Bags** (filled gold, dark text) and **Explore Bridal Collection** (outlined, transparent fill).
- Both scroll to `#collections`.

## Change 3 — 4-icon USP trust strip
`UspStrip` already sits directly under the hero, above the grid. Its four items will be updated to your copy:
Sacred Bhagwan Vastra / Handstitched / Eco-Conscious / Ships Across India, with icons temple, needle-thread, leaf, truck, and subtexts as written — shipping subtext reads "Free shipping above ₹999". Layout becomes icon-above-text, one row on desktop, 2×2 on mobile.

## Change 4 — Shop by Occasion
Already live with the exact four tiles and copy you listed, in a grid linking to `/#collections`. No content change needed; only verification that all four link correctly and render 2×2.

## Change 5 — Brand story section
`BhagwanVastraStory` already exists between the tiles and the FAQ. Its heading and body will be set to your exact text ("The Story Behind Bhagwan Vastra" + the Kiran Mam / Sampurna NGO paragraph).

## Change 6 — FAQ accordion
The homepage FAQ accordion already contains all 7 of your questions (plus 5 extra brand questions). Edits: retitle the section heading to "Frequently Asked Questions", reorder your 7 to the top, align answer wording with your text, and keep ₹999 in the shipping answer.

## Change 7 — SEO footer copy
`SeoContentBlock` already runs above the site footer. Its H2 becomes "Potli Bags for Every Sacred Occasion" and the body is replaced with your paragraph (ending "Free shipping above ₹999").

## Change 8 — Structured data
Organization schema already renders sitewide; it will be checked/completed with email, telephone +919220464425, and the Rohini / Delhi / IN address. The homepage FAQPage schema will be regenerated from the final FAQ list so the 7 Q&As match the visible accordion exactly.

## Change 9 — Remove Lovable badge
Turn the badge off via publish settings. This requires a Pro plan or higher — if the project isn't on Pro, the toggle will fail and I'll tell you rather than hacking it out of the page.

## Change 10 — Sitemap & robots.txt
Both already exist. `public/robots.txt` allows all crawlers and already includes `Sitemap: https://punarvsu.com/sitemap.xml` — no change. `public/sitemap.xml` is generated by `scripts/generate-sitemap.mjs`; it will be regenerated so every public page (home at priority 1.0, about, contact, blog + posts, sacred-knowledge, studio, shipping, privacy, terms, all product handles) is current, with admin/auth excluded.

## Technical notes
- Files touched: `src/components/HeroSection.tsx`, `UspStrip.tsx`, `FAQSection.tsx`, `BhagwanVastraStory.tsx`, `SeoContentBlock.tsx`, `StructuredData.tsx`, `scripts/generate-sitemap.mjs` → `public/sitemap.xml`.
- Colors use existing gold/ivory design tokens; no hardcoded hex.
- JSON-LD is injected client-side, which Google executes; social-preview crawlers only read the static head.
